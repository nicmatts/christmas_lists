<h1>View a wish list</h1>

<table>
<% @people.each do |person| %>
  <tr>
    <td><%= link_to(person.name, {:action => 'show', :id => person.id }, :class =>'action show') %></td>
  </tr>
  <% end %>
</table>
<br>
 <%= link_to("Add to wish list", {:controller => 'gift', :action => 'new'}, :class => 'pull-right') %>
